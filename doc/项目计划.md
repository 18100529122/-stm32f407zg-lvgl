# STM32F407 LVGL Demo 项目开发计划

## 1. 项目概述
本项目旨在在正点原子探索者 (Alientek Explorer) STM32F407 开发板上实现一个基于 LVGL (Light and Versatile Graphics Library) 的图形界面演示程序。利用开发板的 FSMC 接口驱动 16 位并行 LCD 和外部 SRAM，结合 FreeRTOS 实时操作系统，展示 LVGL 的流畅交互与丰富组件。

## 2. 硬件资源配置
根据 [README.md](file:///c:/work/github/-stm32f407zg-lvgl/README.md) 的引脚说明，核心硬件配置如下：

### 2.1 显示与触摸
*   **LCD 显示屏**: FSMC 16位并行接口
    *   `LCD_CS`: PG12 (FSMC_NE4)
    *   `LCD_RS`: PF12 (FSMC_A6)
    *   `LCD_WR`: PD5 (FSMC_NWE)
    *   `LCD_RD`: PD4 (FSMC_NOE)
    *   `LCD_BL`: PB15 (背光控制)
    *   `LCD_D0~D15`: PD/PE 端口数据线
*   **触摸屏接口**:
    *   电容屏 (GT9147): I2C 接口
    *   引脚: `CT_SCL` (PB0), `CT_SDA` (PF11), `CT_INT` (PB1), `CT_RST` (PC13)
    *   注意: 触摸引脚与电阻屏 SPI 接口复用，需确保硬件跳线或配置正确。

### 2.2 存储与辅助
*   **外部 SRAM**: 1MB (XM8A51216V33A)，通过 FSMC 接口访问，用于 LVGL 的显存缓冲区和动态内存分配。
    *   `SRAM_CS`: PG10 (FSMC_NE3)
*   **系统状态指示**:
    *   `LED0`: PF9 (系统心跳)
    *   `LED1`: PF10 (状态指示)
*   **调试与通信 (注意引脚复用)**:
    *   `UART1`: PA9/PA10 (115200bps，主要调试接口)
    *   **复用风险说明**: 
        *   `PA2/PA3`: 在以太网 (RMII_MDIO)、RS232 (USART2) 和 PWM_DAC 之间复用，开发时需通过 P4 跳线确认模式。
        *   `PD0/PD1`: CAN1 与 FSMC 地址线/控制线存在复用风险，需确保 FSMC 配置不冲突。

## 3. 软件架构
*   **底层驱动**: STM32Cube HAL 库
*   **操作系统**: FreeRTOS
*   **图形库**: LVGL (v8.x 或更高版本)
*   **开发工具**: STM32CubeIDE

## 4. 开发阶段规划 (基于代码现状更新)

### 第一阶段：基础环境与硬件驱动 (已由 CubeMX 完成配置)
1.  **[已完成] 系统基础配置**: 168MHz 主频、LED0/1、UART1 (调试串口) 已配置。
2.  **[已完成] FSMC 基础初始化**: 
    *   Bank 3 (SRAM) 和 Bank 4 (LCD) 硬件接口已初始化。
    *   `fsmc.c` 中 Bank 3 的 `WriteOperation` 已设置为 `ENABLE`。
3.  **[已完成] 补全显示与触摸配置**:
    *   **PB15** (LCD_BL) 已配置。
    *   **PB0, PF11, PB1, PC13** (GT9147 I2C/INT/RST) 已配置。
4.  **[已完成] FreeRTOS 基础环境**: 任务调度器已初始化，`defaultTask` 栈空间已扩展至 4KB。

### 第二阶段：硬件功能验证 (当前阶段)
1.  **[待完成] 外部 SRAM 验证**:
    *   编写测试函数对 1MB SRAM 进行全地址读写校验，确保硬件连接可靠。
2.  **[待完成] LCD 显示验证**:
    *   实现底层 `LCD_WriteReg` / `LCD_WriteData`。
    *   读取 LCD ID，编写初始化序列。
    *   实现简单的清屏测试 (刷入不同颜色)。
3.  **[待完成] 触摸屏验证**:
    *   实现软件 I2C 驱动，读取 GT9147 版本号及坐标数据。

### 第三阶段：LVGL 集成与中间件准备
1.  **[待完成] LVGL 源码集成**: 
    *   将 LVGL 核心库添加到工程中。
    *   配置 `lv_conf.h` (颜色深度 16bit，启用外部内存管理)。
2.  **[待完成] 外部 SRAM 内存管理**: 
    *   实现基于外部 SRAM 的内存分配器，供 LVGL 动态组件及双缓冲区使用。

### 第四阶段：接口对接与 Demo 开发
1.  **显示与输入对接**:
    *   实现 `lv_port_disp` (FSMC 加速) 和 `lv_port_indev`。
2.  **Demo 展示**: 
    *   加载官方 Benchmark 或自定义仪表盘 UI。
    *   性能调优 (时序优化及帧率测试)。
