# STM32F407 LVGL Demo 项目开发计划

## 1. 项目概述
本项目旨在在正点原子探索者 (Alientek Explorer) STM32F407 开发板上实现一个基于 LVGL (Light and Versatile Graphics Library) 的图形界面演示程序。利用开发板的 FSMC 接口驱动 16 位并行 LCD 和外部 SRAM，结合 FreeRTOS 实时操作系统，展示 LVGL 的流畅交互与丰富组件。

## 2. 硬件资源配置
根据 [README.md](file:///c:/work/github/-stm32f407zg-lvgl/README.md) 的引脚说明，核心硬件配置如下：

### 2.1 显示与触摸
*   **LCD 显示屏**: FSMC 16位并行接口
    *   `LCD_CS`: PG12 (FSMC_NE4)
    *   `LCD_RS`: PF12 (FSMC_A6)
    *   `LCD_WR`: PD5 (FSMC_NWE)
    *   `LCD_RD`: PD4 (FSMC_NOE)
    *   `LCD_BL`: PB15 (背光控制)
    *   `LCD_D0~D15`: PD/PE 端口数据线
*   **触摸屏接口**:
    *   电容屏 (GT9147): I2C 接口
    *   引脚: `CT_SCL` (PB0), `CT_SDA` (PF11), `CT_INT` (PB1), `CT_RST` (PC13)
    *   注意: 触摸引脚与电阻屏 SPI 接口复用，需确保硬件跳线或配置正确。

### 2.2 存储与辅助
*   **外部 SRAM**: 1MB (XM8A51216V33A)，通过 FSMC 接口访问，用于 LVGL 的显存缓冲区和动态内存分配。
    *   `SRAM_CS`: PG10 (FSMC_NE3)
*   **系统状态指示**:
    *   `LED0`: PF9 (系统心跳)
    *   `LED1`: PF10 (状态指示)
*   **调试与通信 (注意引脚复用)**:
    *   `UART1`: PA9/PA10 (115200bps，主要调试接口)
    *   **复用风险说明**: 
        *   `PA2/PA3`: 在以太网 (RMII_MDIO)、RS232 (USART2) 和 PWM_DAC 之间复用，开发时需通过 P4 跳线确认模式。
        *   `PD0/PD1`: CAN1 与 FSMC 地址线/控制线存在复用风险，需确保 FSMC 配置不冲突。

## 3. 软件架构与分层
本项目采用层次化设计，确保代码的高内聚与低耦合，具体分层如下：

*   **App 层 (`Core/App`)**: 存放业务逻辑、LVGL 界面代码及自定义 Shell 命令。
*   **Middleware 层 (`Core/Middleware`)**: 
    *   **LVGL**: 图形库核心。
    *   **EasyLogger**: 引入 [EasyLogger](https://github.com/armink/EasyLogger)，实现分级日志打印 (LogDebug, LogInfo, LogWarn, LogError)。
    *   **letter-shell**: 引入 [letter-shell](https://github.com/NevermindZZT/letter-shell)，通过串口实现交互式命令行终端。
    *   **FreeRTOS**: 实时操作系统内核。
*   **BSP 层 (`Core/BSP`)**: 板级支持包，包括 LCD、SRAM、GT9147 触摸屏、ADC (1Msps)、DAC 等底层硬件驱动，并提供高精度延时工具。
*   **驱动层**: STM32Cube HAL 库。

## 4. 开发阶段规划 (基于代码现状更新)

### 第一阶段：基础环境与硬件驱动 (已由 CubeMX 完成配置)
1.  **[已完成] 系统基础配置**: 168MHz 主频、LED0/1、UART1 已配置。
2.  **[已完成] FSMC 基础初始化**: Bank 3 (SRAM) 和 Bank 4 (LCD) 已初始化。
3.  **[已完成] 补全显示与触摸配置**: 包含背光、I2C 引脚及中断。
4.  **[已完成] FreeRTOS 环境**: 任务调度器已初始化。

### 第二阶段：基础组件与硬件验证 (已完成)
1.  **[已完成] 基础工具开发**:
    *   **[已完成] 串口通信 (BSP_USART)**: 
        *   实现 USART1 + DMA 循环接收 (IDLE 中断)，支持不定长数据。
        *   实现 `printf` 重定向。
    *   **[已完成] 延时功能**: 
        *   在 BSP 层实现了稳健的指令级 `delay_us`，确保硬件初始化阶段时序准确。
    *   **[进行中] 日志系统**: 准备移植 **EasyLogger**。
    *   **[待完成] Shell 终端**: 移植 **letter-shell**。
2.  **[已完成] 硬件功能验证**:
    *   **[已完成] LCD 验证**: 实现 ILI9806 驱动，完成横屏 (800x480) 初始化。
    *   **[已完成] 触摸屏验证**: 
        *   完成 0x9806 电容触摸屏 (GT9xxx) 驱动适配。
        *   实现软件 I2C 驱动并与 CubeMX 引脚宏同步。
        *   在 FreeRTOS 任务中实现 50Hz 实时坐标扫描与打印。

### 第三阶段：LVGL 集成与内存管理 (已完成)
1.  **[已完成] LVGL 源码集成**: 
    *   将 LVGL 9.2.2 核心库添加到 `Core/Middleware/lvgl`。
    *   精简配置 `lv_conf.h`，禁用未使用的组件和示例，显著减少 Flash 占用。
2.  **[已完成] 外部 SRAM 内存池**: 
    *   在外部 SRAM (0x68040000) 建立 128KB 内存池供 LVGL 使用。
3.  **[已完成] OS 适配层**:
    *   实现 `lv_freeRTOS.c/h`，建立独立的 LVGL 刷新线程（5ms 周期）。
    *   处理线程安全，使用 `lv_lock/unlock` 互斥机制保护 UI 操作。

### 第四阶段：接口对接与 App 开发 (进行中)
1.  **[已完成] 接口对接**: 
    *   已实现 `lv_port_disp` (800x480 横屏)。
    *   已实现 `lv_port_indev` (对接 GT9xxx 电容触摸)。
2.  **[已完成] 模拟外设扩展**:
    *   **ADC 采样**: 实现 1Msps 高速硬件触发采集 (Timer 2 TRGO)，通过 DMA 循环模式降低 CPU 负载。
    *   **数据处理**: 实现 DMA 传输完成中断回调，自动计算缓冲区均值。
    *   **DAC 输出**: 建立基础波形输出框架。
3.  **[进行中] 应用展示**: 
    *   [已完成] 基础 UI 框架搭建，包含图表、标签及交互按钮。
    *   [已完成] 事件驱动机制：实现按钮点击数字递增逻辑 (`lv_ui_event.c`)。
    *   [已完成] 控件库扩展：已在 `lv_conf.h` 中启用所有常用控件（Slider, Switch, Roller 等）及布局引擎 (Flex/Grid)。
3.  **[待完成] 业务功能**: 开发更复杂的业务演示界面。
